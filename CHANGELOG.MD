# Changelog

## persona-system — 2026-05-16

Plan: directory-per-persona bundles under `prompts/<name>/` with TOML manifest, swappable prompts and config overrides, wired later through pipeline and `HecklerConfig`. Full decomposition: `.dev/plans/persona-system/plan.md`.

- **T1 (Persona module):** Added **`heckler/persona.py`** — **`Persona`** dataclass, **`load_persona`**, **`list_personas`**, **`apply_persona_overrides`**, **`PersonaNotFoundError`**, stdlib **`tomllib`** loading, §2 TOML→**`HecklerConfig`** flattening (including **`pacing_interval` → `min_output_interval_s`**), **`[output].comment_types`** omitted from overrides, and contract logging on load / unknown override keys. **`+`** **`tests/test_persona.py`**. **Rationale:** Centralizes persona bundle I/O and override merge so later tasks (reactor, config env, pipeline) share one mapping surface. **Decision log:** `.dev/decision-logs/persona-system-T1.md`.

- **T2 (Prompt migration):** Moved **`prompts/system.md`** and **`prompts/examples.json`** into **`prompts/heckler/`** via **`git mv`**, added **`prompts/heckler/persona.toml`** with default **Heckler** persona metadata and §2 TOML keys matching **`HecklerConfig`** defaults (no **`[output]`** section). **`+`** **`tests/test_persona_prompt_bundle.py`**. **Rationale:** Establishes the directory-per-persona bundle layout so **`load_persona`** and pipeline wiring can resolve **`prompts/<persona>/`** without maintaining duplicate root-level prompt copies. **Deferred (audit-visible, packet T2 risks):** full **`pytest`** still fails in **`tests/test_reactor.py`** until **T3** removes filesystem reads from **`Reactor.__init__`** and **T6** retargets **`test_examples_json_types_are_comment_type_members`** — expected mid-DAG breakage, not a T2 implementation defect. **Adversarial gap:** shipped **`examples.json`** “**`type`**” strings vs **`CommentType`** membership is not re-asserted here (still owned by **`test_examples_json_types_are_comment_type_members`** after **T6**).

- **T4 (Config persona selection):** Added **`HecklerConfig.persona_name`** (default **`heckler`**) and **`load_config()`** parsing of **`HECKLER_PERSONA`** with strip / empty fallback matching **`HECKLER_LLM_MODEL`**. Updated **`.env.example`** and **`tests/test_models.py`** (defaults, env override, whitespace-only default, empty-string default, surrounding-whitespace trim). **Rationale:** Exposes persona id on the typed config surface so **T5** can resolve **`prompts/<persona>/`** consistently with operator env setup. **Deferred (adversarial §2.1):** full-tree **`pytest`** may still fail for the **T2**/**T3**/**T6** reactor/examples path; **`tests/test_models.py`** is green for this slice.

- **T5 (Pipeline persona wiring):** **`heckler/pipeline.py`** now parses **`--persona`**, resolves **`prompts/<name>/`** via **`load_persona`**, applies **`apply_persona_overrides`** before **`HecklerLogger`** and heavy model setup, constructs **`Reactor(config, persona.system_prompt, persona.examples)`**, and exits **`1`** with **`[HECKLER] Error:`** on **`PersonaNotFoundError`**. **`tests/test_pipeline.py`** updates shutdown-thread mocks for the three-arg reactor, adds CLI override / not-found / prompt-pass-through / post-override-config falsifiers. **Rationale:** Connects persona bundles and config overrides to the live pipeline without touching capture or transcription modules.

- **T6 (Docs, packaging notes, examples path test):** **`tests/test_reactor.py::test_examples_json_types_are_comment_type_members`** now reads **`prompts/heckler/examples.json`** and asserts the file lists at least one example so **`CommentType`** membership is not vacuously satisfied on an empty list. **`README.md`** documents **`HECKLER_PERSONA`**, the **`--persona`** CLI flag, and that prompt bundles are resolved from the repo / editable install layout (non-editable **`pip install .`** does not ship **`prompts/`**). **`pyproject.toml`** left unchanged after verification that **`[tool.setuptools.packages.find]`** intentionally scopes Python packages only and prompt assets remain a repo-root data directory. **Rationale:** Aligns the regression test and operator docs with the **`prompts/<persona>/`** layout without adding setuptools **`package_data`** until a real distribution story needs it.

## transcription-engine — 2026-05-16

Plan: transcribe-only pipeline mode (AudioCapture + Whisper persistence, CLI `--mode`, VAD overrides) without persona-mode regressions. Full decomposition: `.dev/plans/transcription-engine/plan.md`.

- **T1 (Transcript SQLite store):** Added **`heckler/transcript_store.py`** with **`TRANSCRIPT_SCHEMA_VERSION`**, **`TranscriptSession`** / **`TranscriptChunk`**, **`init_transcript_schema`**, **`create_session`**, **`close_session`**, **`insert_chunk`**, **`get_session`**, and **`get_chunks`** for tables **`transcript_sessions`**, **`transcript_chunks`**, and **`transcript_schema_version`** (separate from **`heckler_schema_version`**; no FK to **`events`**). Callers reuse **`heckler.event_store.open_store`** for connections. **`tests/test_transcript_store.py`** covers init/version rejection, session lifecycle, chunk ordering, FK failure on unknown session, and double-close idempotency. **Rationale:** Isolates transcript DDL from event-store migrations while keeping one DB file per config. **Decision log:** `.dev/decision-logs/TE-T1.md`. **Deferred:** forward migrations when **`TRANSCRIPT_SCHEMA_VERSION`** increments are **`RuntimeError`** only until a follow-on defines upgrade steps.

- **T2 (Markdown exporter):** Implemented **`export_session_markdown`** in **`heckler/transcript_store.py`** — reads **`get_session`** / **`get_chunks`**, builds **`# {name} — {date}`** plus **`[HH:MM:SS]`** lines from **`started_at`**, writes via temp file + **`os.replace`** (atomic overwrite) and **`mkdir(parents=True)`** on the output parent; logs export at **INFO** / **ERROR** on **OSError**; **`RuntimeError`** when the session id is unknown. Extended **`tests/test_transcript_store.py`** with format, empty-session, nested path, missing-session, and invalid **`timestamp_iso`** falsifiers. **Rationale:** Incremental full-file snapshots during transcribe sessions need a deterministic, reviewable artifact without widening the SQLite schema.

- **T3 (Config extensions):** Extended **`HecklerConfig`** with **`mode`**, transcribe VAD defaults (**`transcribe_max_speech_duration_s`**, **`transcribe_silence_duration_ms`**, **`transcribe_min_speech_duration_ms`**), **`transcripts_dir`**, and **`session_name`**; **`load_config()`** reads **`HECKLER_MODE`**, **`HECKLER_SESSION_NAME`**, and **`HECKLER_TRANSCRIPTS_DIR`** with strip / empty fallbacks consistent with other **`HECKLER_*`** env keys. **`+`** **`tests/test_config.py`**. **Rationale:** Gives **T4** a typed config surface for transcribe-mode wiring without scattering env reads outside **`config.py`**. **Deferred (audit-visible):** **`load_config()`** does not validate **`mode`** against **`{persona,transcribe}`** — **T4** argparse / dispatch owns that contract.

- **T4 (Pipeline mode split + CLI):** **`heckler/pipeline.py`** — **`--mode {persona,transcribe}`** (CLI **`None`** defers to **`config.mode`**), **`--session-name`**, transcribe branch with **`open_store`** + **`init_transcript_schema`**, **`create_session`** / **`close_session`**, **`_run_transcribe_worker`** ( **`Transcriber.transcribe`** + **`insert_chunk`** under a lock, stdout **`[TRANSCRIBE]`** lines), **`dataclasses.replace`** VAD overrides into **`AudioCapture`**, permanently-clear **`threading.Event()`** for **`is_playing`**, shutdown **`export_session_markdown`** to **`{transcripts_dir}/{session_label}.md`** with logged swallow on export/DB write failure; persona **`else`** path unchanged. Extended **`tests/test_pipeline.py`** (worker persistence / empty skip / sentinel, **`main --mode transcribe`** does not construct Speaker/Reactor/logger/gates, session-name forwarding, VAD override capture). **Rationale:** Delivers transcribe-only operation without loading LLM/TTS while preserving persona-mode behavior. **Decision log:** `.dev/decision-logs/TE-T4.md`. **Deferred (adversarial §2.1):** no falsifier for **`transcribe_thread.join` timeout** while the worker is wedged (same class as long-standing persona join coverage).

- **T5 (Transcribe worker integration tests):** Extended **`tests/test_pipeline.py`** with two-chunk sequence persistence, a wrapped **`insert_chunk`** call-count falsifier, **`main`** coverage for **`create_session`** / **`close_session`** on transcribe shutdown, explicit **`Transcriber`** construction in transcribe mode, **`HecklerConfig.mode == transcribe`** when the CLI omits **`--mode`**, persona regression (**`main([])`** and **`--mode persona`**) asserting **`Speaker`** / **`Reactor`** construction, and **`--list-devices`** short-circuit when **`--mode`** is also present. **Rationale:** Locks the T4 integration surface to §2 CLI and worker behavior without coupling tests to private wiring beyond public **`insert_chunk`** patching.

- **T6 (Auditor remediation + tracked plan artifacts):** **`git add`** scoped to **`.dev/plans/transcription-engine/`** ( **`plan.md`**, **`context-map.md`**, **`packets/T1.md`–`T6.md`** ), **`.dev/decision-logs/TE-T1.md`**, and this **`CHANGELOG.MD`** entry so **`git show HEAD:<path>`** resolves the §8.2 plan bundle; **`plan.md` §8** marked **Complete** with §8.1 bundle introducer **`Tree SHA`** resolved by `git log -1 --format=%H -- .dev/plans/transcription-engine/plan.md` (reflexive on that detached checkout) and clean-tree **`python -m pytest tests/`** transcript (**202 passed**, exit **0**); **`context-map.md`** Phase 0.5 baseline uses the same introducer rule; **`TE-T1.md`** banner closes audit **F1** (exporter supersession). **Rationale:** Unblocks orchestrator §8.2 artifact archaeology (**P1**–**P3**) without sweeping unrelated **`.dev/`** deletes. **F4** (invalid non-empty **`HECKLER_MODE`** via env only): still **deferred** — argparse **`--mode`** owns the **`{persona,transcribe}`** contract; **`load_config()`** remains permissive (see **T3** bullet above); **`.dev/decision-logs/TE-T4.md`** was out of this subtask’s **Files to touch**, so the F4 note is not duplicated there. **Adversarial gap:** no automated check that **`.dev/plans/transcription-engine/**`** stays tracked on future branches (process invariant, not pytest-covered).

## sqlite-event-decomposition-analytics — 2026-05-11

Plan: decompose persisted `events.payload_json` into relational structure for post hoc SQL analytics while preserving `HeckleEvent` JSON contracts and the legacy JSONL import path. Full decomposition: `.dev/plans/sqlite-event-decomposition-analytics/plan.md`.

- **T1 (Architecture decision freeze):** Landed owner resolutions for context-map Flags 1–6 in `.dev/decision-logs/T20-event-decomposition-arch.md` — normalized SSOT for analytics with `serialize_heckle_event` / `heckle_event_from_json_dict` still defining field semantics; automatic `init_schema`-path migration with pytest fixture coverage; eval-adjacent tables in the same DB file; dual “eval” vocabulary with doc disambiguation owned by T5; **required** pytest for import/dedupe/insert paths touched by decomposition; `reactor_result` in child table **`event_reactor_results`** (FK `event_id`); dataset-eval table name **`heckler_eval_labels`**. **Rationale:** Removes executor guesswork on SSOT, migration, and test policy before T2–T5 land DDL and writers. **Decision log:** `.dev/decision-logs/T20-event-decomposition-arch.md`. **+** `tests/test_t20_event_decomposition_architecture_log.py` (flags 1–6 **Landed** presence guard).

- **T4 (Legacy JSONL import dedupe + insert parity):** **`scripts/import_legacy_jsonl.py`** now dedupes **`--skip-existing`** with **`COALESCE(utterance_id, json_extract(payload_json, '$.utterance_id'))`** (same **`$.timestamp_iso`** path) so skips match v2 normalized rows and legacy JSON-only rows, and inserts use the same normalized **`events`** columns plus **`event_reactor_results`** as **`insert_heckle_event_row`**, still under one commit per **`import_lines`** batch. **Rationale:** Aligns import with T21 SSOT and logger shape (T20 Flag 5). **`+`** **`tests/test_import_legacy_jsonl.py`**. **Deferred:** falsifier for SQLite builds **without** JSON1 **`OperationalError`** branch would need a custom build; error message path remains manual.

- **T5 (Docs + retired-string sweep):** **`heckler/pipeline.py`** module docstring now describes SQLite persistence instead of JSONL logging; **`README.md`** adds legacy-import CLI (`python scripts/import_legacy_jsonl.py` with **`--database`** / **`-d`**, **`--dry-run`**, **`--skip-existing`**) and disambiguates pacing **`cooldown_remaining_at_eval`** from dataset-style **`heckler_eval_labels`** (T20 Flag 4). **`.dev/plans/sqlite-event-decomposition-analytics/context-map.md`** and **`plan.md`** (hidden couplings) plus **`packets/T4.md`** coupling note updated so **`SCHEMA_VERSION`** and import/insert paths match post-T2 code (**`insert_heckle_event_row`**, schema v2). **Deferred:** no pytest asserts README or module docstring prose—doc-only slice; falsifier would need a dedicated doc contract test outside this packet’s primary touch list.

- **T3 (Logger write path + transactional denormalized insert):** Added **`insert_heckle_event_row`** in **`heckler/event_store.py`** so live logging writes **`payload_json`** (still built from **`serialize_heckle_event`**) **and** v2 normalized **`events`** columns plus **`event_reactor_results`** when present, all in **one commit**. **`HecklerLogger.log_event`** now calls that helper under the existing lock. **Rationale:** Matches T21/T20 physical SSOT for analytics without partial parent/child writes on failure. **Deferred:** child-insert fault injection to assert rollback clears the parent row (see adversarial note in **`tests/test_context_buffer_and_logger.py`**).

## sqlite-local-db-obs-langfuse

Plan: replace append-only JSONL under `**log_dir**` with **SQLite** as the sole steady-state event store (stdlib `**sqlite3`**), add reactor-local **LiteLLM** tracing hooks and correlation into persisted rows, and keep scope minimal (no eval UI, no audio in DB). Full decomposition and auditor handoff (plan **v1.1.0**, §8): `.dev/plans/sqlite-local-db-obs-langfuse/plan.md`.

**T1:** Added `**heckler/event_store.py`** (`**open_store`**, `**init_schema**`, `**insert_event_row**`, `**events**` + `**heckler_schema_version**` DDL, WAL / busy-timeout pragmas for single-process multi-threaded use) and `**heckler/tracing_context.py**` (thread-local `**str` → `str**` correlation for reaction-thread LiteLLM metadata). Tests in `**tests/test_event_store.py**`. **Decision log:** `.dev/decision-logs/T12.md`.

**T2:** Retired `**HecklerConfig.log_dir`**; added `**sqlite_database_path`** (default `**logs/heckler.db**`) and `**load_config()**` parsing of `**HECKLER_DATABASE_PATH**` — value is stripped and overrides the default only when non-empty after strip (whitespace-only env falls back to default; `**test_load_config_without_llm_keys**` clears `**HECKLER_DATABASE_PATH**` assert default path). `**heckler/logger.py**` uses `**Path(sqlite_database_path).parent**` for `**mkdir(parents=True)**`; internal naming moved off `**log_dir**`. Steady-state event persistence is SQLite-only after **T3** (no interim JSONL). `**tests/test_models.py**`: defaults, `**HECKLER_DATABASE_PATH**` override in `**test_load_config_env_overrides**`, whitespace falsifier `**test_load_config_heckler_database_path_whitespace_falls_back_to_default**`. `**tests/test_context_buffer_and_logger.py**`: `**HecklerConfig(sqlite_database_path=...)**` instead of `**log_dir**`, `**test_logger_creates_parent_directory_for_sqlite_path**` (replaces `**test_logger_log_dir_created**`). No edits needed for `**tests/test_audio_capture.py**` / `**tests/test_pipeline.py**` (**no `log_dir` references**). No new dependencies. **Decision log:** `.dev/decision-logs/T13.md`.

**T3:** `**HecklerLogger`** is SQLite-only for steady-state events: opens the DB at `**config.sqlite_database_path`**, initializes schema once, and inserts under the existing `**threading.Lock**` so the shared `**sqlite3**` connection ( `**check_same_thread=False**`, per **T12**) avoids writer contention. Payload uses `**serialize_heckle_event`** + `**json.dumps`** only (removed `**_serialize` / `_coerce_json**`, daily `**heckler_*.jsonl**` paths, and `**_path_for_date**`). Optional `**tracing_context**` is written to `**correlation_json**` and `**clear_correlation()**` runs in `**finally**` so metadata does not leak across `**log_event**` calls; insert failures log at **ERROR** and re-raise. **Rationale:** one JSON shape contract with `**heckler/models.py`**, correlation lifecycle localized to the logging path, and write failures remain visible to callers. Tests rewritten in `**tests/test_context_buffer_and_logger.py`** (including failure-path and correlation-clearing falsifiers). Deferred: explicit `**Connection.close()**` on pipeline shutdown (see **T14**). **Decision log:** `.dev/decision-logs/T14.md`.

**T4:** `**Reactor.react`** clears `**tracing_context`** before each `**litellm.completion**` and again on completion exceptions; after a successful return, correlation uses only primitive values from the response (`**id**` → `**completion_id**`, `**model**`, `**system_fingerprint**`, and `**_hidden_params**` `**litellm_call_id**` / `**model_id**` when dict-backed) so placeholder mocks do not leak into `**correlation_json**`. Hosted-observability scaffold: when env indicates Langfuse (`**LANGFUSE_PUBLIC_KEY**` + `**LANGFUSE_SECRET_KEY**`) or LangSmith-style tracing (`**LANGCHAIN_TRACING_V2**` / `**LANGSMITH_TRACING**` with `**LANGCHAIN_API_KEY**` / `**LANGSMITH_API_KEY**`), `**litellm.completion**` receives `**metadata**` (`**generation_name**`: `**heckler.react**`, tags `**heckler**` / `**heckler-reactor**`) without mutating global LiteLLM callback lists (single instrumentation surface; avoids duplicate trace roots). **Rationale:** LiteLLM ≥1.40 forwards `**metadata`** to logging/callback integrations operators enable separately; heckler adds reaction-thread correlation for SQLite joins. LLM failures remain `**(None, latency_ms, DiscardReason.LLM_ERROR)**` with provider-agnostic error logging. **Tests:** `**tests/test_reactor.py`** — `**metadata**` present/absent by env, correlation fields from explicit response attrs, MagicMock attr falsifier, stale correlation cleared on API exception. No new dependencies. **Decision log:** `.dev/decision-logs/T15.md`.

**T5:** Closed documentation and tests deferred from **T2**: **README** now describes SQLite (`**logs/heckler.db**` default) as the sole steady-state event sink, documents **`HECKLER_DATABASE_PATH`** and retired **`log_dir`** / daily **`heckler_*.jsonl`** paths (historical only), replaces the old “append to JSONL” usage line with SQLite + stdout banners, adds **`LOG_DENSITY_FAILURES`** semantics (density rejects go through the same SQLite **`HeckleEvent`** path when enabled), and adds an informational observability table (`**LANGFUSE_PUBLIC_KEY**` / `**LANGFUSE_SECRET_KEY**`; `**LANGCHAIN_TRACING_V2**` or `**LANGSMITH_TRACING**` with `**LANGCHAIN_API_KEY**` or `**LANGSMITH_API_KEY**`). **`.env.example`** gains commented **`HECKLER_DATABASE_PATH`** and the same tracing vars (optional; no-op when unset). Corrected the sqlite-plan **T2** bullet in this changelog that still claimed interim JSONL post-**T3**. **`tests/test_models.py`**: added **`test_config_has_sqlite_path_not_log_dir`** (regression guard on **`HecklerConfig`** fields). No further **`log_dir`** / JSONL test drift found; full pytest green (**113** tests).

**T6:** Added **`scripts/import_legacy_jsonl.py`** — one-shot import of legacy **`heckler_YYYY-MM-DD.jsonl`** lines into **`events`** using **`heckle_event_from_json_dict`** validation and the same payload path as live logging (**`serialize_heckle_event`** + **`json.dumps`** + **`insert_event_row`** with **`correlation_json=NULL`**). **`--skip-existing`** dedupes by **`(utterance_id, timestamp_iso)`** via **`json_extract`** on **`payload_json`**; **`--dry-run`** validates without writes; reads JSONL as **`utf-8-sig`** so a UTF-8 BOM does not break **`json.loads`**. Invoked as **`python scripts/import_legacy_jsonl.py`** (no **`project.scripts`** entry). Manual verification checklist is in the script docstring. **Deferred:** no dedicated pytest module for the script (T6 **files to touch** was **`scripts/`** only); failure mode “SQLite build without JSON1 when using **`--skip-existing`**” is documented (script exits **2** with a clear message).

## litellm-provider-switch

Plan: migrate the heckler LLM integration from the Anthropic Python SDK to **LiteLLM** so operators can switch providers (OpenAI, Anthropic, Ollama, and other LiteLLM backends) via configuration and environment variables, default chat model **GPT-4o mini** (`openai/gpt-4o-mini`), without changing the rest of the pipeline. Full decomposition and contracts: `.dev/plans/litellm-provider-switch/plan.md`.

**T1:** Introduced LiteLLM-oriented config: default model `**openai/gpt-4o-mini`**, optional multi-provider env keys via `**getenv`** (`ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `OLLAMA_API_BASE`), and `**HECKLER_LLM_MODEL**` override for `**llm_model**`—eliminating mandatory `**KeyError**` when `**ANTHROPIC_API_KEY**` is unset at load time. Added runtime dependency `**litellm**`. **Decision log:** `.dev/decision-logs/T10.md`.

**T2:** Replaced Anthropic `**messages.create`** usage with `**litellm.completion`**, OpenAI-style `**messages**` (system + user), provider-aware `**api_key**` / `**api_base**` kwargs from `**HecklerConfig**`, provider-agnostic `**LLM API call failed**` error logging, and `**completion_assistant_text**` for extracting assistant text from LiteLLM/OpenAI-shaped responses. Removed `**anthropic**` from `**pyproject.toml**`; `**tests/test_reactor.py**` mocks `**litellm.completion**`. **Decision log:** `.dev/decision-logs/T11.md`.

**T3:** Extended `**tests/test_reactor.py`** with explicit regression coverage for plain-text extraction from OpenAI-shaped completions (`**choices[0].message.content`** as `**str**`, Flag 3 / T11) and for `**LLM_ERROR**` when the mocked completion returns `**None**` or an empty `**choices**` list. Monkeypatch target remains `**litellm.completion**` per T11.

**T4:** Updated `**.env.example`** and `**README.md`** for LiteLLM multi-provider setup, default `**openai/gpt-4o-mini**`, and env keys aligned with `**load_config()**` (`HECKLER_LLM_MODEL`, `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `OLLAMA_API_BASE`, plus existing Whisper/Kokoro/pacing vars). Appended **Landed: post-T4 errata** to `**.dev/decision-logs/T7.md`** and `**T9.md`** so Anthropic-only / pipeline-wrapper narrative is not read as current behavior. Synced `**heckler_seed.md**` (§3, §4.5, §5, §9) to match shipped config and reactor contracts.

**T5 (amendment, plan v1.2):** After audit `**.dev/audits/2026-05-09-litellm-provider-switch.md`** (**F1** blocking), rewrote `**.dev/decision-logs/T7.md`** and `**T9.md`** with **Current behavior** vs fenced **Historical** sections so top-level prose matches shipped LiteLLM / direct `**react`** wiring; folded errata into current narrative. Aligned `**.dev/decision-logs/T11.md`** and `**README.md**` for `**azure/**` + `**OPENAI_API_KEY**`. Plan **§2** *Landed* line documents `**openai/`** / `**azure/`** `**api_key**` routing.

## heckler-v1

**T10:** Scoped setuptools package discovery in `pyproject.toml` (`[tool.setuptools.packages.find]` / `include = ["heckler*"]`) so `pip install -e .` does not treat `prompts/`, `tests/`, and other sibling dirs as top-level packages. Widened `Reactor.react()` to `tuple[Optional[ReactorResult], float, Optional[DiscardReason]]` (third element `None` on success, `LLM_ERROR` on API/parse failure, `SCORE_GATE` below threshold); removed `_install_reactor_discard_tracking` / `_react_with_discard` and the pipeline import of `_extract_text_content`, so the reaction worker reads discard semantics from `react()` directly. Updated `tests/test_reactor.py` and `tests/test_pipeline.py` (including `_run_reaction_worker` coverage for direct `reactor.react` calls and pacing rejection after a successful react).

**T9:** Added `heckler/pipeline.py` (`main`, threaded capture → transcribe → semantic gate → reaction queue with drop-oldest overflow matching `audio_capture`, reaction worker with `pacing_gate.record_output()` strictly before `speaker.speak()`, `_react_with_discard` wrapper for §2 `LLM_ERROR` vs `SCORE_GATE`, graceful shutdown via sentinels + joins), `heckler/__main__.py` for `python -m heckler`, argparse `--list-devices`, stdout startup banners with timings, `README.md` (CUDA torch install, `.env` vars from `.env.example`, usage, first-run downloads), and `tests/test_pipeline.py`. **Decision log:** `.dev/decision-logs/T9.md`. **Deferred:** full threaded shutdown fuzz under artificial queue floods — covered indirectly via `_put_shutdown_sentinel` unit test; expand if CI gains deterministic stress hooks.

**T8:** Implemented `heckler/speaker.py` — Kokoro `KPipeline(lang_code="a")` with `logger.info("downloading TTS model")` before construction, `Speaker.is_playing` mic gate (`set` before synthesis, `clear` after playback via `finally` or on synthesis/`_collect_audio` failure), `sounddevice.play(..., samplerate=24000, blocking=True)`, tuple-shaped Kokoro chunks validated in `_collect_audio`, and `SpeakerError` wrapping synthesis failures. Tests in `tests/test_speaker.py`. **Deferred:** unit assertion that `speak()` return value equals synthesis-only wall time (not playback) — low risk given `perf_counter` boundaries in code; add if latency metrics become contract-tested.

**T2:** Added `heckler/semantic_gate.py` with seed-aligned `STOPWORDS`, `compute_density()`, and `passes_gate()` (lexical density plus `min_word_count` / `density_threshold` from `HecklerConfig`); phrase `you know` is removed with `\byou\s+know\b` before tokenization (per packet risk). Tests in `tests/test_semantic_gate.py`.

**T5:** Implemented `heckler/transcriber.py` (`Transcriber`) around faster-whisper with mandatory `vad_filter=True` and `condition_on_previous_text=False`, startup `[HECKLER]` load logs, and `run()` that emits stripped transcripts including empty strings for silence artifacts. Tests in `tests/test_transcriber.py` mock `WhisperModel` so CI stays GPU-free. **Deferred:** malformed `in_queue` items not asserted — wire contract assumes valid `AudioChunk` inputs from capture.

**T6:** Added `PacingGate` with `evaluate` / `record_output()` (thread-safe lock, cooldown via `min_output_interval_s`, high-score bypass when `score >= score_override_threshold`). Docstrings document calling `record_output()` immediately before TTS (`speaker.speak()`), per T9 coupling surface. Tests in `tests/test_pacing_gate.py` use monkeypatched `time.time`, concurrent `evaluate`, interleaved stress, and an elapsed-equals-interval boundary falsifier.

**T3:** Added `ContextBuffer` (thread-safe rolling transcript window) and `HecklerLogger` (append-only daily JSONL, `HeckleEvent` serialization with enum coercion and defensive `audio_chunk` stripping, path recomputed per event for calendar rollover). Tests in `tests/test_context_buffer_and_logger.py`. **Deferred:** asserting failure behavior when the log path is not writable (permissions / disk full) — integration concern once pipeline wiring exists.

**T4:** Added `heckler/audio_capture.py` with `AudioCapture(config, out_queue, is_playing)` — Silero VAD via `torch.hub.load` and streaming `VADIterator`, 512-sample / 16 kHz frames (current Silero requirement; the seed’s 480-sample note is superseded by upstream), `sounddevice` callback feeding a bounded deque drained in `_capture_loop`, `is_playing` gate before enqueue, drop-oldest on queue overflow, and max-speech force flush with a fresh iterator. `**tests/test_audio_capture.py`** covers overflow policy, sample-rate guard, emit gating, and dtype validation. **Packet note:** T4 listed only `audio_capture.py`; tests and this changelog line follow the executor test mandate and repo `tests/test_<module>.py` convention.

**T1:** Introduced the heckler package with shared models (JSON serialization with enum values and defensive `audio_chunk` stripping), frozen `HecklerConfig` and `load_dotenv`-aware `load_config()`, seed-aligned `pyproject.toml` and `.env.example`, plus `tests/test_models.py`. **Deferred:** explicit tests for invalid enum strings in `heckle_event_from_json_dict` (`ValueError`) — reserved for reactor/logger integration behavior in later tasks.

**T7:** Added `heckler/reactor.py` (`Reactor`, `_parse_response` with JSON + regex fallback `r'\{[^}]+\}'`, synchronous non-streaming Anthropic calls, score gate `score < score_threshold`), verbatim `prompts/system.md` and `prompts/examples.json`, and `tests/test_reactor.py` with mocked client behavior. **Decision log:** `.dev/decision-logs/T7.md`.