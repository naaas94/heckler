# Executor Packet — T7: Tests + README + `heckler_arg` TOML + `persona_builder` SKILL

**Plan:** `persona-speech-reload` · **Packet:** T7  
**Executor skill:** [executor-subtask-execution](../../../skills/executor-subtask-execution/SKILL.md)  
**Spec (binding):** `PERSONA_SPEECH_RELOAD.md` §14 (P0–P1 gaps), §19 (scenario matrix S1–S14), §20 (exit criteria), §18.7–18.9

---

## §1. Task statement (verbatim)

All code subtasks (T1–T6) have landed. T7 delivers: the full scenario matrix test suite covering S1–S14, replacement of the old "swap never rebuilds" test, verification of `heckler_arg` TOML correctness, README update, and `persona_builder/SKILL.md` voice table update.

**Non-goals:** No new code features. Documentation and tests only (plus TOML hygiene).

---

## §2. Shared contracts (verbatim)

### Mandatory test replacement (binding)
`tests/test_controller.py::test_swap_persona_does_not_change_transcriber_whisper_language` **must be deleted**. Replace with a same-sig / cross-sig matrix covering S2 (cross-locale reload), S4 (same-locale no reload), S13 (re-select same persona no-op).

### Scenario matrix (binding — S1–S14 from spec §19)

| # | Scenario | Expected behavior |
|---|---|---|
| S1 | GUI boot, combo=`heckler_arg`, Start | es TTS + es Whisper |
| S2 | Running `heckler` → `heckler_arg`, Reload | stop/load/start, es |
| S3 | Running `heckler` → `heckler_arg`, Cancel | no Reactor change; en TTS; combos reverted |
| S4 | Running `heckler` → `technician`, both en | swap only, no reload |
| S5 | `heckler` → persona with `en-gb` | reload (`b` vs `a`) |
| S6 | Locale override `es`, persona en, Start | reload to es |
| S7 | Transcribe running → Persona mode | Speaker loaded with persona locale |
| S8 | Persona change during initial load | one load or Start correction |
| S9 | `locale=es`, `af_sarah` | warn; reload still uses `e` |
| S10 | Reload fails (CUDA) | revert UI, error dialog |
| S11 | Rapid en/es/en persona spam | mutex: second change blocked during reload |
| S12 | CLI `--persona heckler_arg` | es at load |
| S13 | Re-select same persona | no-op (no reload, no double swap) |
| S14 | `switch_mode` + persona + locale | consistent signature |

All S1–S14 must have at least a mock-level unit test. Manual verification required for S1–S3 (spec §20 items 1–3).

### P0 gaps (all must be addressed)

| ID | Gap | Test |
|---|---|---|
| G1 | KPipeline `lang_code=es` after reload path | `tests/test_speaker.py::test_kokoro_lang_code_es_after_reload` |
| G2 | `switch_mode` transcribe→persona when Speaker None | `tests/test_controller.py::test_switch_mode_to_persona_ensures_speaker` (T2 delivered this; T7 verifies) |
| G3 | `locale_override` consistent on `load_models` and `start` | `tests/test_controller.py::test_target_speech_config_consistent` |
| G4 | Startup race combo change vs `ModelLoadThread` | `tests/test_gui.py::test_startup_race_persona_change_during_load` |
| G5 | `locale=es` + `af_sarah` valid but broken | `tests/test_gui.py::test_voice_locale_mismatch_warning_logged` |
| G6 | Tests assert swap never changes transcriber | Replaced by S2/S4 matrix |

### P1 gaps (ship or document)

| ID | Gap | Action |
|---|---|---|
| G7 | `currentTextChanged` on populate fires reload | Covered by `blockSignals` (T3/T4); test that no reload fires during init |
| G8 | Rapid changes double reload | S11: mutex test |
| G9 | `load_models` fails after stop | S10: failure revert test |
| G10 | Cancel leaves inconsistent UI | S3: cancel revert test |
| G11 | CLI has no hot-swap | Document in README (no reload needed; existing) |
| G12 | `HECKLER_LOCALE` vs "From persona" | `tests/test_config.py` — precedence table baseline already exists; add note in README |
| G13 | Mode toggle during reload | S11 mutex covers disable; document Stop-during-reload in README |

### Tests framework
- `pytest`; no new test files (extend existing)
- Mock all heavy model I/O with `unittest.mock.MagicMock`
- Use `pytest.mark.parametrize` for same-sig / cross-sig matrix

### Verification command (binding)
```bash
pytest tests/test_locale.py tests/test_config.py tests/test_controller.py tests/test_gui.py tests/test_pipeline.py tests/test_persona.py tests/test_speaker.py -q
```

---

## §4. Subtask spec

**Scope:** Add scenario matrix tests S1–S14. Replace old swap test. Verify/update `heckler_arg/persona.toml`. Update `README.md`. Update `.cursor/skills/persona_builder/SKILL.md`.

**Files to touch:**
- `tests/test_controller.py` — delete old test, add S2/S4/S5/S13/S14 + G2/G3
- `tests/test_gui.py` — add S1/S3/S6/S7/S8/S9/S10/S11 + G4/G5/G7/G8/G10/G11
- `tests/test_pipeline.py` — add S12 (already partially in T6; T7 adds integration-style mock)
- `tests/test_speaker.py` — add G1 (`lang_code=es` after mock reload)
- `tests/test_locale.py` — verify `speech_stack_signature` tests exist (added in T1; T7 may extend for edge cases)
- `prompts/heckler_arg/persona.toml` — verify `kokoro_voice = "ef_dora"`, `locale = "es"`; update if wrong
- `README.md` — replace "swap never rebuilds" language; add locale/reload documentation
- `.cursor/skills/persona_builder/SKILL.md` — add per-locale Kokoro voice table

**Contract bindings:** All §2 rows above. Mandatory test replacement. Scenario matrix coverage.

**Inputs:**
- T2: controller API finalized
- T3: locale combo available
- T4: ModelLoadThread, Start path
- T5: `_apply_persona_and_speech`, dialog behavior
- T6: CLI path

**Outputs:**
1. Extended test files covering all S1–S14 and P0 gaps.
2. Verified `prompts/heckler_arg/persona.toml`.
3. Updated `README.md`.
4. Updated `.cursor/skills/persona_builder/SKILL.md`.

**Kill criteria:**
1. If `test_swap_persona_does_not_change_transcriber_whisper_language` still exists — halt; CI encodes old contract.
2. If S2 (cross-locale reload triggered) is not covered — halt; P0 gap G6.
3. If G1 (`KPipeline lang_code=es`) test is missing — halt; P0 gap.
4. If `heckler_arg/persona.toml` has `kokoro_voice = "af_sarah"` — halt; D15 violated.
5. If `README.md` still contains "swap never rebuilds" or equivalent language — halt; Surface 7 open.
6. If the verification command fails — halt; do not declare T7 complete.

**Log tier:** `standard`

**Risks & mitigations:**
- S8 (startup race) is a GUI-thread race scenario. Test with: (a) create `ModelLoadThread` with a mock controller; (b) simulate combo change between `__init__` and `run()` by patching `persona_name_fn` to return a different value than what was "baked in" at init; (c) verify `run()` calls the fn at run time (T4's fix). 
- S10 (reload failure): mock `reload_speech_stack_for_persona` to raise; assert `_reloading` is `False` after, error dialog shown, combos reverted.
- `heckler_arg/persona.toml` — context map §Orchestrator handoff notes states `ef_dora` is already present. Verify with a direct read; if already correct, no edit needed (T7 is a no-op for this file).

---

## `heckler_arg` TOML expected state (D15)

```toml
[voice]
locale = "es"
kokoro_voice = "ef_dora"
kokoro_speed = 1.05
```

If current file differs, update it.

---

## README sections to update

1. Find and replace any instance of "swap never rebuilds" or "STT/TTS language remains whatever was fixed at the last `load_models` call" in operator-facing text.
2. Add a "Persona locale and reload" section covering:
   - How `[voice].locale` in persona TOML drives STT/TTS language.
   - Same-locale swap = hot-swap only (no downtime).
   - Cross-locale swap = reload dialog (~20–60 s); or automatic on Start.
   - Speech locale override combo in GUI.
   - CLI: `--persona heckler_arg` loads es automatically.
   - G14 note: voice-only change (same locale) does not trigger reload.
   - G11 note: CLI has no interactive hot-swap.

---

## `persona_builder/SKILL.md` update

Add a table under voice selection guidance:

```markdown
### Per-locale Kokoro voice table

| Locale slug | Kokoro `lang_code` | Compatible voice prefixes | Example |
|-------------|-------------------|---------------------------|---------|
| `en` / `en-us` | `a` | `af_*`, `am_*` | `af_sarah` |
| `en-gb` | `b` | `bf_*`, `bm_*` | `bf_emma` |
| `es` | `e` | `ef_*`, `em_*` | `ef_dora` |

**Rule:** Always match voice prefix to `lang_code`. Mixing (e.g. `af_sarah` with `locale = "es"`) sets the Spanish phonemizer but uses an English voice — sounds wrong. Heckler will warn in the status bar but will not block the reload.
```

---

## §5.2 Load-bearing assumptions (filtered for T7)

5. `(test_swap_persona_does_not_change_transcriber_whisper_language is the only old-contract test | §2 Tests: mandatory replacement | If another test also asserts old contract, T7 may green incorrectly | T7)` — T7 executor: grep for `does_not_change_transcriber` before declaring done.

6. `(heckler_arg/persona.toml already has kokoro_voice = "ef_dora" | spec §24 D15 + context map handoff notes | If file has af_sarah or missing key, S1/S9 tests fail | T7)` — verify by reading file.

---

## §5.4 Hidden couplings (filtered for T7)

None new for T7. All S1–S14 tests are mock-level; no shared test fixtures that could conflict with T1–T6 outputs.

---

## Resolved inputs

All T1–T6 outputs must be landed before T7 begins.

- **T1:** `speech_stack_signature`, `supported_locale_labels` in `heckler.locale`.
- **T2:** Full controller reload API, `SpeechReloadPolicy`, updated `swap_persona`/`switch_mode`.
- **T3:** `_locale_combo`, `selected_locale_override`, locale sync in not-running path.
- **T4:** `selected_persona_name`, F2 fix, `ModelLoadThread` callables, Start path `ensure_heavy_models`.
- **T5:** `_apply_persona_and_speech`, ask dialog, `_reloading` mutex, voice warning, `_reload_speech_btn`.
- **T6:** `pipeline.main()` uses `ensure_heavy_models`.
