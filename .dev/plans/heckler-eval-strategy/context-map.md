# HALT — Overlapping concurrent exploration

**Existing artifact:** `.dev/plans/sqlite-event-decomposition-analytics/context-map.md` explores the same root paths (`heckler/event_store.py`, `heckler/logger.py`, `heckler/models.py`, `heckler/pipeline.py`, `scripts/import_legacy_jsonl.py`) and a largely identical transitive closure (full `heckler/` package, `tests/`, `pyproject.toml`).

**Orchestrator action required:** choose **merge** (single plan), **supersede** (retire or archive the older map for this workstream), or **explicit scope partition** — for example bind schema/migration ownership to the sqlite decomposition plan while binding eval-strategy §7 deliverables (label writer, score-gate persistence, export/stratification scripts, version stamps) to this artifact, with a single shared contract for `SCHEMA_VERSION`, `serialize_heckle_event`, and DDL names frozen in T20/T21.

**Staleness note:** The sqlite plan context map was generated at commit `7d5b1f0b5eb97088b4d4826e868a79dee1bdd4c8`. This map reflects **HEAD** `91687be20a4d2ec707561721bc1777e7eeda39a5`, where `heckler_eval_labels` DDL and v2 normalized inserts are already implemented.

---

# Context map — heckler-eval-strategy

**Generated by:** pre-plan-exploration v0.2  
**Commit SHA:** 91687be20a4d2ec707561721bc1777e7eeda39a5  
**Branch:** master  
**Working tree:** dirty (`??` `.dev/eval-strategy.md`, `.dev/audits/2026-05-11-sqlite-event-decomposition-analytics.md`, `.dev/retrospectives/learning/2026-05-11-sqlite-event-decomposition-analytics.md`, `.dev/retrospectives/methodology/2026-05-11-sqlite-event-decomposition-analytics.md` — planning artifacts; `.dev/eval-strategy.md` is the task spec and is in conceptual scope but not imported by runtime code)  
**Exploration date:** 2026-05-11  
**Task description:** User request: run `/pre-plan` on `.dev/eval-strategy.md`. That document synthesizes persisted-event semantics, eval-relevant gaps, and prioritized recommendations for human-in-the-loop and future automated evaluation of heckler commentary (notably §7: `heckler_eval_labels` usage, optional score-gate candidate persistence, context fingerprint/snippet, version stamps on events, export/stratification tooling, SQLite insert-failure guardrails, calibration slice).  
**Repository root:** `c:\Users\Ale\Documents\Repos\heckler`  
**Readiness verdict:** CONDITIONAL  
**Readiness rationale:** A prior context map overlaps the same code surface; orchestrator must partition or merge plans, and several §7 items require owner decisions (writer location, reactor/pipeline contract for score-gate rows, schema shape for version/context) before subtask packets are frozen.

---

## §Scope boundary

**Root paths explored:**

- `heckler/event_store.py` — Owns `heckler_eval_labels` DDL, `insert_heckle_event_row`, `SCHEMA_VERSION`; any label insert API or new eval columns land here or must stay consistent with migrations.  
- `heckler/logger.py` — `HecklerLogger.log_event` is the steady-state writer path; eval-relevant extensions (non-raising metrics, extra JSON) would intersect here.  
- `heckler/models.py` — `HeckleEvent`, `ReactorResult`, `serialize_heckle_event` / `heckle_event_from_json_dict`; any new persisted eval fields or round-trip changes start here.  
- `heckler/reactor.py` — Score gate discards parsed `ReactorResult` at return boundary; eval-strategy §2/§7 “persist score-gate candidates” implies this module and/or its return contract.  
- `heckler/pipeline.py` — Constructs `HeckleEvent` for all `log_event` paths including `DiscardReason.SCORE_GATE` with `reactor_result=None`.  
- `scripts/import_legacy_jsonl.py` — Parity with live insert SQL and `json_extract` dedupe; export tooling may live alongside as new scripts.  

**Explicit exclusions:**

- `.venv/`, `__pycache__/`, `.git/`, `mcps/**` — tooling and caches.  
- `.dev/plans/**` (other plan trees), `.dev/audits/**` — cited for prior reasoning only.  
- `README.md`, `CHANGELOG.MD` — documentation; not enumerated in file map unless execution touches them.  

**File count in scope:** 30 (15 under `heckler/*.py`, 13 under `tests/*.py` plus `tests/conftest.py`, `scripts/import_legacy_jsonl.py`, `pyproject.toml`)  

**Visited-set size (Phase 1 traversal):** 30  

---

## §File map

| path | role | in_task_scope | rationale |
|------|------|----------------|-----------|
| `heckler/event_store.py` | SQLite DDL, migrations, `insert_*` | direct | `heckler_eval_labels` table and all event write contracts for eval joins. |
| `heckler/logger.py` | `HecklerLogger` → `insert_heckle_event_row` | direct | Single writer path for live events; insert failure behavior noted in eval-strategy §2/§6. |
| `heckler/models.py` | Domain types and JSON serializers | direct | Payload shape for labels, future version/context fields, `ReactorResult`. |
| `heckler/reactor.py` | LiteLLM `Reactor`, score gate | direct | Returns `(None, latency, SCORE_GATE)` without persisting sub-threshold candidate today. |
| `heckler/pipeline.py` | Workers, `HeckleEvent` construction, `main` | direct | All persisted outcomes; only `--list-devices` on CLI (relevant if new eval CLI is mistaken for pipeline CLI). |
| `scripts/import_legacy_jsonl.py` | JSONL → SQLite import | direct | Parity with normalized insert; export may be sibling script. |
| `pyproject.toml` | deps and `heckler` console entry | direct | New script entry points only if scope adds `[project.scripts]` (plans elsewhere freeze pipeline CLI). |
| `heckler/config.py` | `HecklerConfig`, `load_config` | transitive | `score_threshold`, `log_density_failures`, DB path envs affect eval gaps and stratification. |
| `heckler/tracing_context.py` | Thread-local correlation dict | transitive | Feeds `correlation_json` for hosted trace joins per eval-strategy §5. |
| `heckler/context_buffer.py` | In-memory transcript window for reactor | adjacent | Eval-strategy §2/§7 “context not recorded” — fingerprint feature would couple here. |
| `heckler/semantic_gate.py` | Density gate | transitive | `log_density_failures` controls density-gate row presence. |
| `heckler/pacing_gate.py` | Cooldown / `cooldown_remaining_at_eval` | transitive | Pacing “eval” vocabulary vs dataset eval (T20 Flag 4). |
| `heckler/audio_capture.py` | Mic + VAD | transitive | Imported by pipeline only. |
| `heckler/speaker.py` | TTS | transitive | `spoken` / `tts_latency_ms` semantics per eval-strategy §1 footnote. |
| `heckler/transcriber.py` | Whisper wrapper | transitive | Upstream of `transcript`. |
| `heckler/__main__.py` | Shims to `pipeline.main` | transitive | |
| `heckler/__init__.py` | Package version | transitive | |
| `tests/conftest.py` | Pytest fixtures | transitive | |
| `tests/test_event_store.py` | Store + migration tests | transitive | |
| `tests/test_context_buffer_and_logger.py` | Logger persistence | transitive | |
| `tests/test_models.py` | Config + serialization | transitive | |
| `tests/test_pipeline.py` | Pipeline + reaction worker | transitive | |
| `tests/test_reactor.py` | Reactor + score gate | transitive | |
| `tests/test_import_legacy_jsonl.py` | Import dedupe/insert | transitive | |
| `tests/test_t20_event_decomposition_architecture_log.py` | T20 decision string regression | transitive | Asserts architecture log including `heckler_eval_labels`. |
| `tests/test_audio_capture.py` | Audio | transitive | |
| `tests/test_transcriber.py` | Transcriber | transitive | |
| `tests/test_semantic_gate.py` | Semantic gate | transitive | |
| `tests/test_pacing_gate.py` | Pacing | transitive | |
| `tests/test_speaker.py` | Speaker | transitive | |

---

## §Interface inventory

### `heckler/event_store.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `SCHEMA_VERSION` | constant | `int` (= 2) | `init_schema`, tests | suspect_modified | `tests/test_event_store.py` |
| `open_store` | function | `(path: Path) -> sqlite3.Connection` | `HecklerLogger.__init__`, tests, import `main` | suspect_modified | `tests/test_event_store.py` |
| `init_schema` | function | `(conn: sqlite3.Connection) -> None` | `HecklerLogger.__init__`, tests, import | suspect_modified | `tests/test_event_store.py` |
| `insert_event_row` | function | `(conn_or_cursor: sqlite3.Connection \| sqlite3.Cursor, payload_json: str, correlation_json: str \| None = None) -> int` | tests, import tests | unknown | `tests/test_event_store.py`, `tests/test_import_legacy_jsonl.py` |
| `insert_heckle_event_row` | function | `(conn: sqlite3.Connection, *, event: HeckleEvent, payload_json: str, correlation_json: str \| None = None) -> int` | `HecklerLogger.log_event`, tests | suspect_modified | `tests/test_event_store.py`, `tests/test_context_buffer_and_logger.py` |
| `_EVENT_ANALYTICS_COLUMNS` | constant | `tuple[tuple[str, str], ...]` (column name, SQL decl pairs) | `_heckle_event_analytics_params`, `insert_heckle_event_row`, `import_legacy_jsonl._insert_imported_event` | suspect_modified | via import script tests |
| `_heckle_event_analytics_params` | function | `(event: HeckleEvent) -> tuple` | `insert_heckle_event_row`, import script | suspect_modified | `tests/test_event_store.py`, `tests/test_import_legacy_jsonl.py` |

*(No `insert_eval_label` or similar public writer exists yet; `heckler_eval_labels` is DDL-only.)*

### `heckler/logger.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `HecklerLogger` | class | `__init__(self, config: HecklerConfig) -> None`; `log_event(self, event: HeckleEvent) -> None` | `pipeline.main` / workers, tests | suspect_modified | `tests/test_context_buffer_and_logger.py`, `tests/test_pipeline.py` |

### `heckler/models.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `CommentType` | enum | members: `SARCASM`, `CALLBACK`, `OBSERVATION`, `ABSURDIST`, `PASSIVE_AGGRESSIVE`, `UNKNOWN` | reactor, models, pipeline | unknown | `tests/test_models.py`, `tests/test_reactor.py` |
| `DiscardReason` | enum | `DENSITY_GATE`, `SCORE_GATE`, `PACING_GATE`, `LLM_ERROR`, `TTS_ERROR` | pipeline, models, reactor | suspect_modified | `tests/test_models.py`, `tests/test_pipeline.py`, `tests/test_reactor.py` |
| `AudioChunk` | class | fields: `audio: np.ndarray`, `captured_at: float` | transcriber, pipeline types | unknown | `tests/test_audio_capture.py`, … |
| `Utterance` | class | `utterance_id`, `transcript`, `semantic_density`, `transcribed_at`, `audio_chunk` | pipeline, reactor | unknown | `tests/test_reactor.py` |
| `ReactorResult` | class | `comment`, `score`, `comment_type`, `raw_response` | reactor, pipeline, serializers | suspect_modified | `tests/test_reactor.py`, `tests/test_models.py` |
| `HeckleEvent` | class | fields per dataclass (see source lines 52–65) | pipeline, logger, tests | suspect_modified | `tests/test_models.py`, `tests/test_pipeline.py`, `tests/test_context_buffer_and_logger.py` |
| `serialize_heckle_event` | function | `(event: HeckleEvent) -> dict[str, Any]` | logger, tests, import | suspect_modified | `tests/test_models.py`, `tests/test_context_buffer_and_logger.py` |
| `heckle_event_from_json_dict` | function | `(d: dict[str, Any]) -> HeckleEvent` | import script, tests | suspect_modified | `tests/test_models.py`, `scripts/import_legacy_jsonl.py` |
| `heckle_event_json_round_trip` | function | `(event: HeckleEvent) -> HeckleEvent` | tests | unknown | `tests/test_models.py` |

### `heckler/reactor.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `completion_assistant_text` | function | `(response: Any) -> str` | tests (import), reactor internals | unknown | `tests/test_reactor.py` |
| `Reactor` | class | `__init__(self, config: HecklerConfig) -> None`; `react(self, utterance: Utterance, context_block: str) -> tuple[Optional[ReactorResult], float, Optional[DiscardReason]]` | `pipeline._run_reaction_worker`, tests | suspect_modified | `tests/test_reactor.py` |

### `heckler/pipeline.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `_run_transcription_worker` | function | keyword-only params including `config`, `heckler_logger`, queues, `transcriber` | internal thread | suspect_modified | `tests/test_pipeline.py` |
| `_run_reaction_worker` | function | keyword-only params including `reactor`, `heckler_logger`, queues, `context_buffer`, `pacing_gate`, `speaker` | internal thread | suspect_modified | `tests/test_pipeline.py` |
| `_execute_spoken_reply` | function | `(pacing_gate: PacingGate, speaker: Speaker, comment: str) -> float` | `_run_reaction_worker` | unknown | `tests/test_pipeline.py` |
| `main` | function | `(argv: Optional[list[str]] = None) -> None` | console script, `__main__`, tests | suspect_modified | `tests/test_pipeline.py` |

### `heckler/config.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `HecklerConfig` | class | fields include `score_threshold`, `score_override_threshold`, `sqlite_database_path`, `log_density_failures`, `context_window_size`, LLM/TTS fields (see source) | logger, pipeline, reactor, gates, tests | suspect_modified | `tests/test_models.py`, … |
| `load_config` | function | `() -> HecklerConfig` | `pipeline.main`, tests | unknown | `tests/test_models.py` |

### `heckler/tracing_context.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `set_correlation` | function | `(metadata: Mapping[str, str] \| None) -> None` | reactor, tests | unknown | `tests/test_reactor.py`, `tests/test_context_buffer_and_logger.py` |
| `get_correlation` | function | `() -> dict[str, str] \| None` | logger, tests | unknown | `tests/test_context_buffer_and_logger.py`, `tests/test_reactor.py` |
| `clear_correlation` | function | `() -> None` | reactor, logger, tests | unknown | same |
| `reset_correlation` | function | `() -> None` | none_found in heckler package | unknown | none_found |

### `heckler/context_buffer.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `ContextBuffer` | class | `__init__(self, maxlen: int) -> None`; `push(self, transcript: str) -> None`; `get_context_block(self) -> str` | `pipeline._run_reaction_worker`, tests | unknown | `tests/test_context_buffer_and_logger.py` |

### `heckler/semantic_gate.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `STOPWORDS` | constant | `frozenset[str]` | `compute_density`, tests | unknown | `tests/test_semantic_gate.py` |
| `compute_density` | function | `(text: str) -> float` | `passes_gate` | unknown | `tests/test_semantic_gate.py` |
| `passes_gate` | function | `(text: str, config: HecklerConfig) -> tuple[bool, float]` | `pipeline._run_transcription_worker`, tests | unknown | `tests/test_semantic_gate.py` |

### `heckler/pacing_gate.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `PacingGate` | class | `__init__(self, config: HecklerConfig) -> None`; `evaluate(self, score: float) -> tuple[bool, float]`; `record_output(self) -> None` | `pipeline._run_reaction_worker`, tests | unknown | `tests/test_pacing_gate.py`, `tests/test_pipeline.py` |

### `heckler/audio_capture.py`, `heckler/speaker.py`, `heckler/transcriber.py`, `heckler/__main__.py`, `heckler/__init__.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| *(package wiring and device/TTS/STT helpers)* | — | types: partial annotations in modules | pipeline / tests | unknown | respective `tests/test_*.py` |

### `scripts/import_legacy_jsonl.py`

| symbol | kind | signature | consumed_by | stability | test_file |
|--------|------|-----------|-------------|-----------|-----------|
| `import_lines` | function | `(conn: sqlite3.Connection, lines: list[str], *, dry_run: bool, skip_existing: bool) -> tuple[int, int, int, int]` | `main` | suspect_modified | `tests/test_import_legacy_jsonl.py` |
| `main` | function | `(argv: list[str] \| None = None) -> int` | `__main__` | suspect_modified | import tests indirect |
| `_insert_imported_event` | function | `(cur: sqlite3.Cursor, event: object, payload_json: str) -> int` — `event` unannotated but used as `HeckleEvent` | `import_lines` | suspect_modified | `tests/test_import_legacy_jsonl.py` |

---

## §Prior reasoning

Decision logs in `.dev/decision-logs/` that reference files in scope:

| log path | subtask | landed decision (1 line) | open deferrals (1 line) |
|----------|---------|--------------------------|-------------------------|
| `.dev/decision-logs/T20-event-decomposition-arch.md` | T20 / arch | **Flag 1 — Landed:** **Normalized tables/columns are canonical** for analytics-relevant event data. JSON in `payload_json` is **optional, deprecated, or removed only where this log explicitly documents** breaking changes, migration, and operator-facing steps. | T21 owns exact table/column list; T5 owns doc grep for eval vocabulary. |
| `.dev/decision-logs/T20-event-decomposition-arch.md` | T20 / arch | **Flag 3 — Landed:** Eval-adjacent data lives in the **same SQLite file** as pipeline events; **new tables** are allowed. **Join keys** (e.g. `events.id`, `utterance_id`, correlation IDs) must be **named in T21** and mirrored in §2 of the plan after schema lands. | Writers for `heckler_eval_labels` not specified in T20 text. |
| `.dev/decision-logs/T20-event-decomposition-arch.md` | T20 / arch | **Flag 4 — Landed:** **Both** meanings of “eval” in scope: (a) post-hoc / dataset / hosted-style evaluation labels and metadata, and (b) runtime pacing gate terminology (e.g. `cooldown_remaining_at_eval`). **Docs and SQL object names must disambiguate** — **T5** owns grep + narrative consistency. | — |
| `.dev/decision-logs/T20-event-decomposition-arch.md` | T20 / arch | **Flag 6 — Landed:** **`reactor_result` is stored in a child table** named **`event_reactor_results`**, keyed **`event_id`** → `events.id`. Nested JSON for reactor fields is **not** the canonical analytics source once the child table lands. | — |
| `.dev/decision-logs/T21-event-decomposition-schema.md` | T21 / schema | **Chosen approach:** **`SCHEMA_VERSION` = 2** … **`heckler_eval_labels`:** `event_id` FK, `label_name`, optional `label_value`, optional `extra_json`, `created_at` — reserved for dataset-style labels (T20 Flag 3); **empty until writers land.** | Header states T3/T4 deferrals were satisfied at HEAD; confirm against current code when planning new writers. |

---

## §Coupling surfaces

**Grep patterns checked:**

- Pydantic model imports — **none** (dataclasses only); pattern checked, N/A.  
- Shared config / env keys: `SCORE_THRESHOLD`, `HECKLER_DATABASE_PATH`, `LOG_DENSITY_FAILURES`, `PACING_INTERVAL` — hits in `heckler/config.py`, `tests/test_models.py`.  
- Bare string / enum wire keys: `score_gate`, `heckler_eval_labels`, `completion_id`, `payload_json`, `correlation_json`.  
- CLI: `--list-devices`, `-d` / `--database`, `--dry-run`, `--skip-existing` (import script).  
- Wire / observability strings: `LANGFUSE_PUBLIC_KEY`, `LANGCHAIN_TRACING_V2`, `litellm_call_id`, `generation_name`, `heckler.react` (reactor metadata).  
- Path-like strings: `logs/heckler.db`, `prompts/system.md`, `prompts/examples.json` (reactor).  
- Decision-log path substring: `.dev/decision-logs` in `event_store.py`, `test_t20_event_decomposition_architecture_log.py`.  
- Message broker verbs `publish` / `emit` / `enqueue` / `send` — **no** external topic/event queue usage in `heckler/` beyond docstring “enqueue” for local `queue.Queue` (`audio_capture.py` only).  

**Surface 1** · confirmed  
```
(events v2 column order and names | contract surface: heckler/event_store.py:_EVENT_ANALYTICS_COLUMNS + heckler/event_store.py:_heckle_event_analytics_params | failure mode: INSERT bind mismatch or wrong column mapping in live vs import | scope areas: import parity, migrations, any new analytics columns)
```

**Surface 2** · confirmed  
```
(JSON round-trip and migration json_extract paths | contract surface: heckler/models.py:serialize_heckle_event fields ↔ heckler/event_store.py:_migrate_v1_to_v2 json_extract paths | failure mode: new HeckleEvent field stays NULL in normalized columns and backfill forever | scope areas: version stamp, context fingerprint, schema bump)
```

**Surface 3** · confirmed  
```
(SCORE_GATE logging shape: reactor_result None vs child table population | contract surface: heckler/pipeline.py:_run_reaction_worker (result is None branch) ↔ heckler/event_store.py:insert_heckle_event_row (child insert when event.reactor_result is not None) | failure mode: if score-gate persistence adds reactor row without aligning passed_score_gate / discard_reason semantics, SQL slices disagree with JSON payload | scope areas: score-gate near-miss persistence, export tooling)
```

**Surface 4** · confirmed  
```
(free-text label_name / label_value | contract surface: heckler/event_store.py:heckler_eval_labels DDL (label_name TEXT NOT NULL, label_value TEXT) | failure mode: aggregate drift and typos per eval-strategy §6 if writers omit validation | scope areas: label CLI/script, documentation)
```

**Surface 5** · confirmed  
```
(correlation_json join keys to hosted traces | contract surface: heckler/reactor.py:_correlation_from_completion_response ↔ heckler/logger.py:log_event correlation_json | failure mode: silent NULL correlation on legacy import rows — eval-strategy §5 already warns; changing key names breaks external joins | scope areas: observability calibration, export joins)
```

**Surface 6** · suspected  
```
(pipeline CLI freeze vs new eval entrypoint | contract surface: heckler/pipeline.py:main argparse vs pyproject.toml [project.scripts] | failure mode: plans that forbid new console scripts without amendment could conflict if eval label tool is added as `heckler` subcommand instead of standalone script | scope areas: label writer packaging)
```
*Disproven by:* explicit plan amendment adding scripts or extending argparse with owner sign-off.

---

## §Ambiguity flags

**Flag 1** · category: ownership  
```
(Where should writes to heckler_eval_labels live — stdlib helper on event_store, standalone script only, or shared library module? | evidence: DDL exists in event_store._ensure_auxiliary_tables; no insert function or script references heckler_eval_labels in Python beyond tests asserting T20 prose | failure mode: duplicate writers, inconsistent transactions, or logger coupling where post-hoc labeling should stay offline | scope areas: eval label writer, export tooling)
```

**Flag 2** · category: vocabulary_collision  
```
(Should “human_quality” / eval label vocabulary be enforced in SQL, in application code only, or in documentation only? | evidence: .dev/eval-strategy.md §3 recommends controlled vocabulary; no symbol human_quality in codebase | failure mode: raters and scripts invent divergent label_name strings; SQL aggregates unusable | scope areas: label writer, documentation)
```

**Flag 3** · category: coexisting_model_versions  
```
(How should score-gate “near miss” rows represent model output — extend Reactor.react return type, add a side channel, or persist inside reactor before return? | evidence: reactor.react returns None with SCORE_GATE after successful parse; pipeline never receives ReactorResult for that path | failure mode: two parsers or divergent score thresholds if pipeline reconstructs JSON from raw text | scope areas: score-gate persistence, reactor, pipeline)
```

**Flag 4** · category: ownership  
```
(Where do version stamps (model id, SCORE_THRESHOLD, prompt hash) physically live — new columns, correlation_json extension, or payload_json only? | evidence: T20 Flag 1 prefers normalized SSOT; eval-strategy §7 item 4 lists multiple options | failure mode: planning Tn against wrong table violates SSOT or duplicates fields | scope areas: version stamp, migrations)
```

**Flag 5** · category: missing_test_coverage  
```
(Is there automated coverage for heckler_eval_labels insert/select? | evidence: grep shows table name in event_store, docstring, and test_t20… string assertions only; no functional label insert test | failure mode: first writer ships without round-trip or FK tests | scope areas: label writer)
```

**Overlap orchestration** · (process; not a schema category)  
```
(How does this plan relate to sqlite-event-decomposition-analytics context-map.md given identical root paths? | evidence: HALT banner at top of this artifact | failure mode: duplicate T2/T3 ownership or contradictory DDL assumptions | scope areas: entire eval-strategy rollout)
```

---

## §Orchestrator handoff notes

- **Implicit contract:** `HecklerLogger.log_event` re-raises on SQLite failure after logging (`logger.py` lines 43–45); eval-strategy §2 calls out tail-loss risk — any “metric without raise” change is a behavior change for callers expecting exceptions.  
- **CHANGELOG / seed docs** reference historical `_react_with_discard`; current `pipeline.py` inlines reaction handling — do not treat CHANGELOG prose as runtime contract without verifying code.  
- **Dirty tree:** `.dev/eval-strategy.md` is untracked; orchestrator should treat it as the normative prose spec once committed.  

---
