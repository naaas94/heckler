# Methodology retrospective — heckler-v1

Personal notes — filed, not for sharing.

## 1. Task identifier

- **Task:** heckler-v1 (HECKLER local reactive audio loop).
- **Plan:** v1.1 — `.dev/plans/heckler-v1/plan.md` (base v1 + **T10** amendment for setuptools discovery and `Reactor.react()` 3-tuple / pipeline de–monkey-patch).
- **Orchestrator / executor skills:** paired plan → packet emission under orchestrator-planning; execution per executor-subtask-execution (inferred from artifact shape).
- **Audit consumed:** `.dev/audits/2026-05-08-heckler-v1.md` (Phase 4 integration seams + decision-log / kill-criterion checks).
- **One line:** Greenfield decomposition through T1→Layer-1 parallel→T9, then a **contract amendment** (T10) after post-execution findings, with a later formal adversarial audit against plan §2 and shipped code.

---

## 2. Plan vs reality

**DAG vs execution:** The staged DAG matched actual sequencing: T1 first; T2–T8 parallel after T1; T9 wiring last among core tasks; **T10 correctly depended on T7 + T9** and ran as an amendment layer. No evidence in the audit of unsafe parallelization within Layer 1.

**Contracts:** Shared §2 in `plan.md` was the anchor; **T10 amended** Types/Interfaces and Error Envelope in-plan (*Landed (T10)*). Code review in the audit largely **passed** Phase 2 compliance for the 3-tuple, setuptools `find`, and pipeline no longer touching `reactor._client`. **Drift showed up in narrative artifacts, not in the frozen §2 tables after amendment:** architectural decision logs **T7.md / T9.md** were still narrating pre-T10 (and in places pre–packet) wiring at audit time (**F-01, F-02**). Changelog under **T9** still described `_react_with_discard` as shipped narrative while **T10** described removal — timeline vs traceability tension (**audit §5 Phase 1 note**).

**Log tiers:** At least one subtask carried **standard** or **architectural** tier (T7, T9 architectural; several **standard**). Tier assignment looks directionally right for blast radius. **Under-tiered** is not the main story; **artifact hygiene after amendment** is.

---

## 3. HALTs and re-plans

**HALTs (executor skill):** No quantitative HALT log was in the audit bundle. The **2026-05-08 audit is report-only** (no execution transcripts). From static evidence: executors **did not** HALT on the tension between T9’s kill criterion (“threads properly joined on shutdown”) and the plan’s **no integration tests in v1** — that gap surfaced only at adversarial audit (**F-03**).

**Re-plans:** At least **one explicit amendment cycle** — **T10** added to close audit findings (packaging + reactor return type / pipeline coupling). That is a **real re-plan**, justified by contract and install correctness.

**Honesty check on silent improvisation:** Shipping **T10** aligned code with amended §2; the audit’s **fail** verdict was driven by **stale decision logs** and **missing join-path tests**, not by silent dismissal of the 3-tuple contract. Where improvisation risk remains is **kill criteria vs test strategy** (join shutdown) — **partial coverage**, not undetected contract cheating.

---

## 4. Adversarial pass calibration

**Rejected alternatives (§5.1):** The decomposition choices (vs monolith, vs deferred test lump, vs merged capture/transcriber) **mattered** for parallel Layer 1 and isolated blast radius; nothing in the audit contradicts that structure.

**Load-bearing assumptions (§5.2):** Audit **Phase 2** did not blow up the Anthropic-centric assumptions in old packets because **LiteLLM migration** later shifted transport (**CHANGELOG** `litellm-provider-switch`, decision-log **Landed** blocks in T7/T9) — a **domain migration outside the original heckler-v1 DAG**, not something §5.2 predicted by name.

**§5.3 highest re-plan risk — T7:** **Partially predictive.** Reactor fragility (parsing, schema) was the **named** technical risk; what actually forced a **planned** amendment was **ambiguous `None` + pipeline monkey-patch vs §2 granularity**, closed by **T10** — same subsystem, different failure mode than “schema drift in the wild.” **T9 coordination risk** (§5.3 note) **surfaced** as integration/seams testing passed (**audit §7 A1–A4, A6 pass**) except **shutdown join** test gap (**F-03**).

**§5.4 hidden couplings:** Audit adversarial table **confirmed** queue policy, `is_playing`, `record_output` ordering, density logging. **Suspected** Kokoro `speed` vs 24 kHz stayed **unknown at doc level** (**A5**) — no re-plan, residual documentation risk.

---

## 5. Methodology gaps surfaced

**Orchestrator:** When an **amendment** (T10) changes §2 and **which decision logs are authoritative**, the plan should **bind** “update or supersede listed decision logs” as **explicit DoD** for the amendment task (or a zero-scope follow-on), not assume executors/changelog carry narrative.

**Executor:** **Kill criteria** that imply **observable behavior** (e.g. shutdown joins) need either **automated coverage**, an explicit **waiver** in the plan with rationale, or a **HALT** when the plan simultaneously forbids broad integration tests — otherwise executors ship **believable** implementations without CI guardrails.

**Contracts schema / process:** “Decision log paths frozen” in §2 is **vestigial without** a **supersession / amendment** rule when §2 rows change after the log was written.

**nothing notable:** Scout-prediction reconciliation table empty — **N/A** without `context-map.md` (greenfield V14 waiver); not a process failure.

---

## 6. Single sentence verdict

**Partially** — the DAG, parallelization story, and **T10** contract amendment **held up** for getting aligned code and an amendable §2, but **methodology leaked** on **keeping architectural logs and tests tied to kill criteria** after amendments and under the stated integration-test constraints.
