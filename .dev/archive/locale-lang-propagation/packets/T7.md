# Executor packet — T7 (locale-lang-propagation amendment)

**Plan:** locale-lang-propagation v1.2.0 · **Subtask:** T7 · **Log tier:** standard · **Audit:** FIND-05

---

## §1 Task statement

Implement a **unified locale knob** on `HecklerConfig` that propagates to faster-whisper (`whisper_language`) and Kokoro (`kokoro_lang_code` on `KPipeline`), with operator entry points via **`HECKLER_LOCALE`** and persona **`[voice].locale`**, plus resolution at config construction so STT and TTS stay aligned. Wire `Speaker` off the hard-coded `lang_code="a"`, close the `load_config()` gap for language env, and fix controller **load-time** binding so `Transcriber`/`Speaker` use the locale effective for the session (including persona merge when `load_models(persona_name=...)` is used). Document that **persona hot-swap does not change STT/TTS language** without a process restart / `load_models` reload.

**Amendment (v1.2.0):** Production **GUI** startup must pass `persona_name` (and `mode`) into `load_models` so persona `[voice].locale` affects STT/TTS before `start()` — closes audit FIND-05.

**Non-goals:**

- New GUI **locale picker** widget (combo remains persona id, not locale slug).
- Rebuilding `Transcriber`/`Speaker` on `swap_persona` or persona-combo change while running.
- Reloading models when user changes persona combo while pipeline is running (Flag 2 — `swap_persona` stays Reactor-only).
- `heckler/gui/main_window.py` structural refactor beyond what T7 requires to pass config into loader.

---

## §2 Shared contracts

| Topic | Contract |
|-------|----------|
| **Types / interfaces** | *(v1.0 — T1–T4 landed; unchanged.)* |
| **Entry-point integration** *(amendment — **T7**)* | **`heckler/gui/app.py` — `ModelLoadThread`:** Constructor accepts `HecklerConfig` (or `persona_name: str | None` + `mode: str`). In `run()`, call `self._controller.load_models(on_progress=on_prog, mode=mode, persona_name=persona_name)` where: `mode = (config.mode or "persona").strip().lower()`; `persona_name = config.persona_name if mode == "persona" else None`. **`main()`:** `loader = ModelLoadThread(controller, config)` (or equivalent). Rationale: `_populate_personas` selects `config.persona_name` before user can Start; combo is disabled until running, so startup persona matches first `start(persona_name=combo)` when unchanged. **Test (owning T7):** `tests/test_gui.py::test_model_load_thread_invokes_load_models_off_thread` — assert `load_models` called with `persona_name=config.persona_name` and `mode=config.mode` (use `call_args.kwargs`). |
| **Error envelope** | Unchanged. |
| **Naming** | Decision log: `.dev/decision-logs/locale-lang-propagation-T7.md` |
| **Logging** | Unchanged. |
| **Tests** | `tests/test_gui.py`; run `pytest tests/test_gui.py -q` |
| **CLI surface** | N/A |

---

## §4 Subtask — T7

| Field | Content |
|-------|---------|
| **ID** | T7 |
| **Scope** | Extend `ModelLoadThread` to call `load_models(on_progress=..., mode=..., persona_name=...)` using `HecklerConfig` defaults. Update `tests/test_gui.py`. Write decision log. |
| **Files to touch** | `heckler/gui/app.py`, `tests/test_gui.py`, `.dev/decision-logs/locale-lang-propagation-T7.md` (new) |
| **Contract bindings** | §2 Entry-point integration (T7), §2 Tests |
| **Inputs** | T4, T5 |
| **Outputs** | GUI wiring; gui test; decision log |
| **Kill criteria** | (1) Halt if FIND-05 unresolved: `ModelLoadThread` still calls `load_models()` with no `persona_name` in persona mode. (2) Halt if executor adds locale picker UI. (3) Halt if `swap_persona` rebuilds Transcriber/Speaker. (4) Halt if persona-combo change while running triggers reload without plan amendment. |
| **Log tier** | `standard` |
| **Risks & mitigations** | Document in decision log: combo disabled pre-start → `config.persona_name` is the effective pre-start persona; `HECKLER_LOCALE` still sets base process locale. |

---

## §5.2 Load-bearing assumptions (filtered)

| Tuple |
|-------|
| (`GUI startup persona matches config.persona_name in combo` \| `main_window._populate_personas` \| wrong persona_name at load if combo logic changes \| T7) |
| (`config.mode transcribe skips persona_name` \| §2 Entry-point T7 \| Speaker skipped via load_models mode \| T7) |

---

## §5.4 Hidden couplings (filtered)

| Tuple | Status |
|-------|--------|
| (`test_model_load_thread only checks on_progress` \| `tests/test_gui.py:124-137` \| must assert persona_name kwarg \| T7) | **confirmed** |
| (`ModelLoadThread had no mode kwarg` \| `app.py:37` \| transcribe GUI may load Speaker unnecessarily today \| T7) | **suspected** — pass `mode=config.mode` to align with CLI; disproven if transcribe already worked |

---

## Resolved inputs

**T4:** `load_models(..., mode=..., persona_name=...)` signature.

**T5:** README notes GUI load before start — align behavior with docs after T7.

---

## Implementation sketch (non-binding)

```python
class ModelLoadThread(QThread):
    def __init__(self, controller: PipelineController, config: HecklerConfig) -> None:
        super().__init__()
        self._controller = controller
        self._config = config

    def run(self) -> None:
        mode = (self._config.mode or "persona").strip().lower()
        persona_name = self._config.persona_name if mode == "persona" else None
        self._controller.load_models(
            on_progress=on_prog,
            mode=mode,
            persona_name=persona_name,
        )
```
