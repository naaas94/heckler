# Executor packet — T6 (locale-lang-propagation amendment)

**Plan:** locale-lang-propagation v1.2.0 · **Subtask:** T6 · **Log tier:** standard · **Audit:** FIND-04

---

## §1 Task statement

Implement a **unified locale knob** on `HecklerConfig` that propagates to faster-whisper (`whisper_language`) and Kokoro (`kokoro_lang_code` on `KPipeline`), with operator entry points via **`HECKLER_LOCALE`** and persona **`[voice].locale`**, plus resolution at config construction so STT and TTS stay aligned. Wire `Speaker` off the hard-coded `lang_code="a"`, close the `load_config()` gap for language env, and fix controller **load-time** binding so `Transcriber`/`Speaker` use the locale effective for the session (including persona merge when `load_models(persona_name=...)` is used). Document that **persona hot-swap does not change STT/TTS language** without a process restart / `load_models` reload.

**Amendment (v1.2.0):** Production **CLI** path must pass the effective `persona_name` into `load_models` when `mode == "persona"`, matching the persona used in `start()` — closes audit FIND-04 / context-map Surface 1 on headless entry.

**Non-goals:**

- New GUI **locale picker** widget or PyQt language controls beyond wiring existing `load_models` / `start` call sites (**T7** owns GUI wiring).
- New CLI flags beyond existing `--persona` / `--mode`.
- LLM “locale” field, Reactor template injection, or automatic register alignment beyond existing prompts.
- Rebuilding `Transcriber`/`Speaker` on `swap_persona` or mode switch without explicit `load_models` reload.
- Auto-detect language from audio (Whisper `language=None` “detect” mode).
- Expanding Kokoro beyond locales listed in §2 mapping table without plan amendment.
- Split per-stack knobs (`whisper_language` vs `kokoro_lang_code` as independent user inputs) — internal fields exist; operators set **`locale`** only.

---

## §2 Shared contracts

| Topic | Contract |
|-------|----------|
| **Types / interfaces** | *(v1.0 — T1–T4 landed; unchanged.)* See plan §2 for `heckler/locale.py`, `HecklerConfig`, `apply_resolved_locale`, persona `[voice].locale`, `PipelineController.load_models(persona_name=...)`. |
| **Entry-point integration** *(amendment — **T6**)* | **`heckler/pipeline.py` `main()`:** After `persona_name = args.persona or config.persona_name` and `mode` resolution, call: `controller.load_models(on_progress=..., mode=mode, persona_name=persona_name if mode == "persona" else None)`. **`start()`** continues `controller.start(mode, persona_name=persona_name, session_name=session_name)` unchanged. **Test (owning T6):** `tests/test_pipeline.py` — scenario A4: persona fixture with `config_overrides={"locale": "es"}` (or TOML tmp) + base config `locale="en"`; assert `load_models` received `persona_name` matching CLI `--persona` / config default, and `Transcriber` (or `load_models` kwargs capture) would use `whisper_language="es"` when mocks allow persona merge. Minimum: `MagicMock` on `PipelineController.load_models` records `persona_name` kwarg on `main([])` persona path. |
| **Error envelope** | Unchanged (`UnsupportedLocaleError`, `PersonaNotFoundError` may surface at load if persona missing). |
| **Naming** | No new symbols. |
| **Logging** | Unchanged. |
| **Tests** | Extend `tests/test_pipeline.py`; run `pytest tests/test_pipeline.py -k locale -q` or targeted new test name after add. |
| **CLI surface** | No new flags; wire existing `--persona` into `load_models`. |

---

## §4 Subtask — T6

| Field | Content |
|-------|---------|
| **ID** | T6 |
| **Scope** | Wire `heckler/pipeline.py` `main()` so `load_models` receives the same `persona_name` used by `start()` when `mode == "persona"`. Add falsifying test mirroring audit scenario A4. |
| **Files to touch** | `heckler/pipeline.py`, `tests/test_pipeline.py` |
| **Contract bindings** | §2 Entry-point integration (T6), §2 Tests |
| **Inputs** | T4 landed (`controller.load_models(..., persona_name=...)`) |
| **Outputs** | CLI wiring; pipeline test |
| **Kill criteria** | (1) Halt if `load_models` still omits `persona_name` on persona-mode `main()` path. (2) Halt if new CLI flags added. (3) Halt if test only mocks `start()` but not `load_models` persona propagation. (4) Halt if transcribe mode passes non-None `persona_name` without plan amendment. |
| **Log tier** | `standard` |
| **Risks & mitigations** | `PersonaNotFoundError` at load vs start — fail-fast at load is acceptable. |

---

## §5.2 Load-bearing assumptions (filtered)

| Tuple |
|-------|
| (`CLI resolves persona_name before load_models` \| §2 Entry-point → `pipeline.py:357-377` \| load_models still uses base config → FIND-04 reproduces \| T6) |
| (`Controller bake API works when persona_name passed` \| §2 Types → `controller.load_models` \| T6 wiring useless if T4 regressed \| T6) |

---

## §5.4 Hidden couplings (filtered)

| Tuple | Status |
|-------|--------|
| (`test_main_persona_flag_overrides_config tests start path only` \| `tests/test_pipeline.py:test_main_persona_flag_overrides_config` \| T6 adds load_models kwarg capture \| T6) | **confirmed** |

---

## Resolved inputs

**T4:** `PipelineController.load_models(..., persona_name: Optional[str] = None)` @ `heckler/controller.py:107-124`.

---

## Implementation sketch (non-binding)

```python
controller.load_models(
    on_progress=lambda msg: print(f"[HECKLER] {msg}", flush=True),
    mode=mode,
    persona_name=persona_name if mode == "persona" else None,
)
```

**Test sketch:** Monkeypatch `PipelineController`, record `load_models` kwargs, invoke `main([])` with `HECKLER_PERSONA` or `--persona`, assert `persona_name` kwarg present.
